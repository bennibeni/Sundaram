export const metadata = {
  title: "Sundaram",
  description: "Sundaram",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
